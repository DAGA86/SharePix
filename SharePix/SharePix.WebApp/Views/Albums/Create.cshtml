@model SharePix.WebApp.Models.Albums.CreateAlbumViewModel

@{
    Layout = "~/Views/Shared/_BasicLayout.cshtml";
}

<h1>@Localize("album.title")</h1>

<section id="fm-Album">
    <form asp-action="Create" id="pic-Album" enctype="multipart/form-data">
        <div asp-validation-summary="ModelOnly" class="text-danger"></div>
        @if (ViewData["SuccessMessage"] != null)
        {
            <div class="text-success">@ViewData["SuccessMessage"]</div>
        }
        @if (ViewData["ErrorMessage"] != null)
        {
            <div class="text-danger">@ViewData["ErrorMessage"]</div>
        }

        <div class="form-group">
            <div class="containerPhoto">
                <div id="preview" class="img"></div>
                <input type="file" name="files" id="photos" multiple />
            </div>
        </div>
        <div class="form-group">
            <label asp-for="Name" class="control-label">@Localize("label.name")</label>
            <input asp-for="Name" class="form-control" />
            <span asp-validation-for="Name" class="text-danger"></span>
        </div>
        <div class="form-group">
            <label asp-for="Description">@Localize("label.description")</label>
            <textarea asp-for="Description" class="form-control" id="exampleFormControlTextarea1" rows="3"></textarea>
        </div>

        <div class="form-style ">
            <input type="submit" value="@Localize("general.button.save")" class="btt" />
            <input type="button" onclick="window.location = '/Home/Index'" value="@Localize("general.button.cancel")" class="btt cancel" />
        </div>



    </form>
</section>

@section Styles{
    <link rel="stylesheet" href="~/css/uploadPhotoAlbum.css">
  }

@section Scripts{
    <script src="/js/uploadPhoto.js"></script>
}